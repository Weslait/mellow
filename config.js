const config = {
    db : {
        host : "localhost",
        user : "root",
        password : "",
        database : "db_mellow"
    },

    initDb : {
        host : "localhost",
        user : "root",
        password : "",
    },
    listPerPage : 10,
};

module.exports = config;